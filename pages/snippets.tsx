const Snippets: React.FC = () => {
  return (
    <div>
      This component(route) we will use as the home page after the user logs in
      Here we gonna show all snippets, created by logged in user
    </div>
  );
};
export default Snippets;
